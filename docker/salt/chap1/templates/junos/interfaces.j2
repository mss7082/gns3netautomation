{%- set interfaces = pillar.interfaces -%}



interfaces {
    {% for interface_dict in interfaces %}
    {% for intf, unit_dict in interface_dict.items() %}
    {{intf}} {
        {% for unit in unit_dict.keys() %}
        unit {{unit}} {
            {% if "ipv4addr" in unit_dict["unit"].keys() %}
            family inet {
                {% for ipv4 in unit_dict["unit"]["ipv4addr"] %}
                address {{ipv4}};
                {% endfor %}
            }
            {% endif %}
                {% if "ipv6addr" in unit_dict["unit"].keys() %}
            family inet {
                    {% for ipv6 in unit_dict["unit"]["ipv6addr"] %}
                address {{ipv6}};
                    {% endfor %}
            }
                {% endif %}
        }
        {% endfor %}
    }
    {% endfor %}
    {% endfor %}
    lo0 {
        unit 0 {
            family inet {
                address {{pillar.loopbackv4}};
            }
        }
    }
    fxp0 {
        unit 0 {
            family inet {
                address {{pillar.mgmtipv4}};
            }
        }
    }
}